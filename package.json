{
  "name": "emoji-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "dotenv -e .env concurrently \"pnpm --filter frontend run dev\" \"pnpm --filter backend run dev\"",
    "build": "pnpm --filter frontend run build && pnpm --filter backend run build",
    "build:fe": "pnpm --filter frontend run build",
    "build:be": "pnpm --filter backend run build",
    "start": "dotenv -e .env concurrently \"pnpm --filter frontend run start\" \"pnpm --filter backend run start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.7",
    "dotenv-cli": "^8.0.0"
  }
}
